<h1>Tasks#show</h1>
<p>Find me in app/views/tasks/show.html.erb</p>
<h2><%= @task.name %></h2>
<h3><%= @task.difficulty %></h3>
<%= link_to "Delete this Task", task_path(id: @task.id), method: :delete, data: {confirm: "Are you sure?"} %>
<%= link_to "Edit this Task", edit_task_path(id: @task.id) %>

